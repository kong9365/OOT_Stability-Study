# 광동 품질·시험 통합 대시보드 — 사용설명서

OOT(이상추세) 빠른 조회 · 안정성 회귀분석(유효기간) · 실시간 메일 알람을 한 화면에서 보는 프로그램입니다. 데이터는 Tableau(`OOT_추출용`)에서 실시간으로 가져옵니다.

> **처음 한 번만** ① Python 설치 → ② `setup.bat` 더블클릭. 이후에는 **`run.bat` 더블클릭만** 하면 됩니다.

---

## 0. 준비물 (꼭 확인)

1. **사내망 연결** — 이 PC가 `tableau.ekdp.com`에 접속 가능해야 합니다(사내 LAN 또는 VPN). 집/외부망만으로는 데이터가 안 나옵니다.
2. **Python 3.10 이상** (아래 1단계에서 설치).
3. **Windows 10/11**.

---

## 1. Python 설치 (이미 있으면 건너뛰기)

1. 인터넷 브라우저에서 **https://www.python.org/downloads/** 접속
2. 노란색 **Download Python** 버튼 클릭 → 내려받은 설치파일 실행
3. ⭐ **설치 첫 화면 맨 아래 `Add Python to PATH` 체크박스를 반드시 체크** 한 뒤 **Install Now** 클릭
4. 설치 완료되면 닫기

> 이미 설치돼 있는지 모르겠으면, 그냥 2단계 `setup.bat`을 먼저 눌러 보세요. Python이 없으면 안내 메시지가 뜹니다.

---

## 2. 설치 (처음 한 번만) — `setup.bat` 더블클릭

- 폴더 안의 **`setup.bat`** 을 더블클릭하세요.
- 검은 창이 뜨고 필요한 부품(패키지)을 자동으로 내려받아 설치합니다. **1~3분** 정도 걸립니다.
- 마지막에 **`[OK] Setup complete`** 가 보이면 성공입니다. 창을 닫으세요.
- 만약 `[ERROR] Python not found` 가 나오면 → 1단계 Python 설치(특히 **Add Python to PATH** 체크)를 다시 하세요.
- 회사 보안/프록시로 설치가 막히면 IT팀에 "사내 PyPI 미러 또는 프록시" 를 문의하세요.

---

## 3. 실행 — `run.bat` 더블클릭

- **`run.bat`** 을 더블클릭하세요.
- 검은 창이 뜨고, **몇 초 후 인터넷 브라우저가 자동으로 열립니다** (`http://localhost:8600`).
- 만약 브라우저에 "연결할 수 없음" 이 잠깐 보이면, **몇 초 기다렸다가 새로고침(F5)** 하세요(서버 준비 중).
- **검은 창은 켜두세요.** 창을 닫으면 프로그램이 꺼집니다.
- 종료하려면 검은 창을 닫거나 그 창에서 `Ctrl + C` 를 누릅니다.

---

## 4. 사용 방법

왼쪽 메뉴에서 페이지를 이동합니다. (좌측 상단 **홈** 버튼/로고를 누르면 처음 화면으로 돌아갑니다.)

### ① OOT 빠른 조회
1. **시험종류** 선택(기본 완제품)
2. **품목코드** 칸에 코드나 품목명 입력 → 목록에서 선택
3. **제조번호(LOT)** 칩 클릭 → 그 LOT의 판정 결과(관리이탈/주의/정상/정성제외)와 항목별 수치가 표시
4. **`📈 이 품목 안정성 분석`** 버튼을 누르면, 그 **품목·그 LOT** 의 안정성 분석으로 바로 이동합니다.

### ② 안정성 회귀분석 (유효기간)
- **시험종류·품목·시험항목** 선택, **함량 규격(%)** 입력, **분석 방식**(통합/배치별 독립) 선택.
- 보여주는 것: 종합 판정(worst-case 유효기간), **추정 저장수명 vs 허가 유효기간** 비교, 합산성 검정(ANCOVA), **저장수명도 그래프**(마우스 올리면 값 표시), 배치별 회귀식·**계산 검증(단계별 수치)**, 두 방식 비교표, 상세 요약표, 시점간 OOT 표.
- **원자료 표** 우측의 **`Excel 다운로드`** 버튼으로 결과를 엑셀(.xlsx)로 저장할 수 있습니다.
- OOT에서 특정 LOT으로 들어오면 상단에 **`배치 OOOOO만 표시 ×`** 칩이 보입니다. **×** 를 누르면 전체 배치 보기로 돌아갑니다.

### ③ 알림 설정 (비밀번호 보호)
- 들어갈 때 **비밀번호**를 묻습니다. **초기 비밀번호: `131103`** (접근할 때마다 입력).
- **발송 정책**(관리이탈만 / 주의 포함), **수신자**(추가·삭제), **조회 주기**, **야간·주말 발송**을 설정.
- 값을 바꾼 뒤 반드시 위쪽 **`💾 저장`** 버튼을 눌러야 저장됩니다(저장 전엔 "저장되지 않은 변경사항" 표시).
- **`✉️ 테스트 발송`** 으로 메일 연동을 확인할 수 있습니다.
- **`🔐 비밀번호 변경`** 에서 진입 비밀번호를 바꿀 수 있습니다.

---

## 5. 동료에게 보여주기 (같은 사내망)

- `run.bat` 검은 창에 표시되는 **`http://<이 PC IP>:8600`** 주소를 동료가 브라우저에 입력하면 같은 화면을 봅니다.
- 접속이 안 되면, 이 PC의 **Windows 방화벽에서 TCP 8600 인바운드 허용** 이 필요합니다(IT팀 문의).

---

## 6. 자주 묻는 문제

| 증상 | 해결 |
|---|---|
| `setup.bat`에서 Python not found | Python 재설치 시 **Add Python to PATH** 체크 |
| 패키지 설치 실패 | 사내망 프록시 영향. 잠시 후 재시도 또는 IT팀에 PyPI 미러 문의 |
| 브라우저가 안 열림 | 직접 `http://localhost:8600` 입력. 검은 창이 켜져 있는지 확인 |
| "품목 목록 로드 실패" / 데이터 안 나옴 | **사내망(VPN) 연결** 확인. Tableau 접속이 안 되는 환경입니다 |
| 알림 비밀번호 분실 | 폴더의 `oot_alert_config.json` 파일 삭제 → 비번이 초기값 `131103`으로 돌아감 |
| 포트 8600이 이미 사용 중 | `run.bat`을 메모장으로 열어 `set PORT=8600` 을 다른 번호(예: 8610)로 변경 |

---

## 7. 폴더 구성 (참고)

```
광동품질대시보드_배포/
├─ setup.bat          ← 처음 1회 설치 (더블클릭)
├─ run.bat            ← 실행 (더블클릭)
├─ 사용설명서.md       ← 이 문서
├─ requirements.txt   ← 설치 패키지 목록
├─ .env               ← 접속·메일 설정 (비밀값 포함, 외부공유 금지)
├─ dashboard_api.py   ← 서버
├─ kdp_core.py        ← 데이터·분석 로직
├─ stability.py       ← 회귀 엔진
├─ lims_parser.py     ← 보조
├─ oot_mail.py        ← 메일 발송
└─ webapp/            ← 화면(index.html, app.js)
```

자동 생성 파일: `oot_alert_config.json`(알림 설정), `.webapp_alarm.json`(주기/야간), `oot_alarm_sent.csv`(발송 이력).

---

## 8. 주의

- `.env` 에는 Tableau 토큰·메일 비밀번호가 들어 있습니다. **폴더를 외부로 공유하지 마세요.**
- 데이터 조회·분석은 참고용입니다. 최종 판정/유효기간은 QC 책임자 검토·승인 및 사내 SOP 확인이 필요합니다.

문의: 품질관리팀
